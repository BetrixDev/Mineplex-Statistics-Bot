{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "globalEnv": ["BOT_TOKEN"],
  "pipeline": {
    "build": {
      "outputs": ["build/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
